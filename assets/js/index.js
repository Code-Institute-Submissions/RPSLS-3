

const pickUserHand = (hand) => {
    console.log(hand);
    // hide current page and show the next page with your choice and the computers choice
    let hands = document.querySelector(".hands");
    hands.style.display = "none";
}